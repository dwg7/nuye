# MMGIS_REVIEW

NASA AMMOSのMMGIS([NASA-AMMOS/MMGIS](https://github.com/NASA-AMMOS/MMGIS)、
Apache 2.0)を、Nuyeの設計にあたって実際に調査した結果。2026-09-07時点、GitHub上の
実リポジトリを直接確認した内容に基づく(推測で埋めていない箇所は「未確認」と明記する)。

## MMGISの描画・共同作業機能

- **Draw Tool**: 点・線・面に加え、矢印・テキストによる注釈もサポートする高度な
  ベクタ編集ツール。実装は`plugins/core/tools/Draw/`にあり、**Leaflet.draw
  (`L.Draw.*`)をベースにしている**(MapLibreではない)
- **ファイル単位の共同編集**: 「ファイル」(`user_files`テーブル)が共同編集の単位。
  所有者・公開範囲(`public`/`publicity_type`)・グループ所有(Lead限定)を持つ
- **リアルタイム同期**: WebSocketベースのインフラ(`specs/003-real-time-collaboration-infrastructure/`)
  で、複数利用者が同じファイルを同時編集できる
- **テンプレート駆動の属性フォーム**: ファイルごとに`template`(JSON)を設定でき、
  slider/number/text/textarea/checkbox/dropdown/incrementer/dateの入力型を組み合わせて
  カスタムフォームを作れる
- **追記専用の操作履歴**: `file_histories`テーブルが、フィーチャーの追加・編集・削除・
  undo・clip(重ね書き)・merge・splitを時系列で記録し、任意時点の状態を復元できる
- 3D地形・可視領域解析・時間対応レイヤー等、Cesiumベースの3D機能も持つ(今回は未調査、
  Nuyeのスコープ外)

## 静的ホスティング可能な部分 / サーバー・DBが必要な部分

**静的ホスティングできない**——これがMMGIS本体についての最も重要な結論。

- Node.js ≥22、PostgreSQL 16+が必須(`package.json`のengines、README badge)
- Draw Toolの共同編集・履歴・公開範囲制御は、すべてPostgreSQL(PostGIS拡張)上の
  テーブル(`user_files`/`file_histories`/`user_features`/`published_stores`/
  `publisheds`)に依存している
- リアルタイム同期はWebSocketサーバーが必要

MMGIS本体を「サーバー側だけ落として静的部分だけ動かす」という選択肢は、上記の理由で
現実的ではないと判断した(起動プロンプト5章の前提どおり)。

## Nuyeで採用した概念

- **フィーチャーへの意味付け**(名称・カテゴリ・状態・観測時刻・出典・次の行動)という
  発想そのもの——MMGISのDraw Toolの属性パネルから着想を得ている
- **「次の行動」を場所に結び付けて残す**という思想(MMGISのファイル/フィーチャー単位の
  ワークフローから)
- Undo/Redoの必要性(MMGISは追記専用履歴として実装しているが、Nuyeは
  `terra-draw`が標準搭載する`draw.undo()`/`draw.redo()`で同等の体験を得ている——
  実装方式は異なるが目指す体験は同じ)

## Nuyeで採用しなかった機能(欠落ではなく意図的な非採用)

- **Leaflet.drawベースの実装そのもの**: MapLibreを使うNuyeとは地図エンジンが異なるため、
  コードは一切参照していない
- **動的テンプレート**: ファイルごとにカスタム属性フォームを定義する仕組みは、
  Nuyeの初期MVPでは過剰と判断し、固定スキーマ1本にした(DECISIONS.md D2)
- **リアルタイム共同編集・WebSocketインフラ**: サーバーを持たない静的MVPの前提と
  相容れないため、初期スコープ外
- **PostgreSQL上の追記専用履歴**: 同上の理由。ブラウザ内保存(LocalStorage)+
  GeoJSON出力という、はるかに単純な永続化に置き換えた
- **公開範囲制御・グループ所有**: 単一利用者・単一端末を前提とする初期MVPには不要

## 将来MMGIS本体を利用する条件

以下のいずれかが必要になった時点で、MMGIS本体(またはその一部)の採用を検討する
価値がある——現時点では条件を満たしていないため、採用しない:

1. **複数利用者による同時編集**が実際の運用上の要件になった場合
   (起動プロンプト23章「D. MMGIS本体」の選択肢)
2. サーバー・データベース運用の体制(可用性・監査・認証)を持てる場合
3. Draw Toolの動的テンプレート機能が必要になるほど、属性スキーマの多様化が
   実際に求められた場合

それまでは、Nuyeの「静的ホスティング+ブラウザ内保存+GeoJSON持出し」という
単純な構成を維持する。

## Open MCTとの役割分担

起動プロンプト4.3の想定アーキテクチャに従う:

```text
Nuye: 描画、属性付与、保存、持出し、再読込——観測・経路・区域・確認状況・次の行動
Open MCT: 時系列、状態監視、テレメトリ、警報
```

Nuyeは「場所に結び付いた知識を書き込む」ことに特化し、時系列・センサー的な状態監視は
scope外とする。将来的な統合は、Nuyeが持つGeoJSON(場所+属性+時刻)をOpen MCT側の
テレメトリソースとして参照させる形が自然だと考えられるが、これは今回のMVPでは
未着手・未検討(具体的な統合方式はまだ無い)。
